# Multilayer-Perceptron-for-Images
Three multilayer perceptron (MLP) models were trained on the MNIST dataset with different activation functions: Rectified Linear Unit (ReLU), LeakyReLU, and Sigmoid.
